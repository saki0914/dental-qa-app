{
  "name": "dental-qa-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Static dental Q&A learning app development helpers.",
  "scripts": {
    "dev": "http-server . -a 0.0.0.0 -p 3000 -c-1",
    "check": "node scripts/check.mjs",
    "emulators:start": "firebase emulators:start --project demo-dental-qa --only auth,firestore,storage",
    "test:unit": "node --test tests/unit/*.test.mjs",
    "test:rules": "firebase emulators:exec --project demo-dental-qa --only firestore,storage \"node --test --test-concurrency=1 tests/rules/*.test.mjs\"",
    "test:migration": "firebase emulators:exec --project demo-dental-qa --only firestore,storage \"node --test tests/rules/dental-assisting-migration.emulator.test.mjs\"",
    "test:migration:ui": "firebase emulators:exec --project demo-dental-qa --only auth,firestore,storage \"playwright test tests/e2e/dental-assisting-migration.spec.mjs --project='iPad Portrait'\"",
    "test:e2e": "playwright test --grep-invert @authenticated",
    "test:e2e:authenticated": "firebase emulators:exec --project demo-dental-qa --only auth,firestore,storage \"playwright test --project='Desktop Chromium' --grep @authenticated\"",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:report": "playwright show-report",
    "test:all": "npm run check && npm run test:unit && npm run test:rules && npm run test:e2e && npm run test:e2e:authenticated",
    "migrate:dental-assisting": "node scripts/migrate_dental_assisting_345_to_350.mjs"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.1",
    "@playwright/test": "^1.61.1",
    "firebase": "12.16.0",
    "firebase-tools": "15.24.0",
    "http-server": "^14.1.1"
  }
}
